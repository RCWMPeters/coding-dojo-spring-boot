CREATE TABLE weather (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    city VARCHAR(255) NOT NULL,
    name VARCHAR(255) NOT NULL,
    timeofrequest TIMESTAMP NOT NULL,
    weatherresponse VARCHAR(1000)
);